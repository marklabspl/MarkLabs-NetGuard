#pragma once
#include <pgmspace.h>

namespace web {
static const char kLogicPage[] PROGMEM = R"HTML(<!doctype html><html lang="pl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>NetGuard · Logic Studio</title><style>
:root{--bg:#050b14;--panel:#0b1829;--line:#1b3853;--text:#edf7ff;--muted:#86a1b8;--power:#f59e0b;--power2:#fb923c;--target:#06b6d4;--target2:#3b82f6;--ok:#22c55e;--bad:#ef4444}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% 0,#102842,transparent 35%),var(--bg);color:var(--text);font:14px system-ui}.wrap{max-width:1240px;margin:auto;padding:20px}.top,.tabs,.head,.actions,.mini{display:flex;align-items:center;gap:10px}.top{justify-content:space-between}.brand{font-size:20px;font-weight:800}.brand .labs{color:#249be9;font-weight:400;letter-spacing:3px}.brand .domain{color:#249be9;font-size:.72em;font-weight:700;letter-spacing:0;margin-left:1px}.tabs{margin:18px 0 25px;padding:6px;background:#071321;border:1px solid var(--line);border-radius:13px;overflow:auto}.tabs a{color:var(--muted);text-decoration:none;padding:10px 14px;border-radius:8px;white-space:nowrap}.tabs .active{color:white;background:#1769aa}.lang,.ghost{background:#10243a;color:#a9dfff;border:1px solid #285170;border-radius:9px;padding:9px 13px}h1{font-size:30px;margin:0}.lead{color:var(--muted);max-width:760px;line-height:1.5}.section{margin-top:32px}.section>.head{justify-content:space-between}.eyebrow{text-transform:uppercase;letter-spacing:.13em;font-size:12px;font-weight:800}.powerText{color:var(--power)}.targetText{color:var(--target)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:14px;margin-top:13px}.card{position:relative;background:linear-gradient(145deg,#0d1e32,#091523);border:1px solid var(--line);border-radius:15px;padding:16px;overflow:hidden}.card.device{border-top:3px solid var(--power)}.card.target{border-top:3px solid var(--target)}.card.rule{border-left:3px solid #8b5cf6}.ico{width:42px;height:42px;border-radius:12px;display:grid;place-items:center;flex:0 0 auto}.device .ico{color:var(--power);background:#f59e0b18}.target .ico{color:var(--target);background:#06b6d418}.ico svg{width:25px;height:25px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}.card h3{margin:0 0 3px}.role{font-size:11px;text-transform:uppercase;letter-spacing:.1em;color:var(--muted)}label{display:block;color:var(--muted);font-size:12px;margin:10px 0 4px}input,select{width:100%;background:#06111e;color:var(--text);border:1px solid #24445f;border-radius:8px;padding:9px}input[type=checkbox]{width:auto}.row{display:grid;grid-template-columns:1fr 1fr;gap:9px}.toggle{margin-left:auto;color:var(--muted)}.help{color:var(--muted);font-size:12px;line-height:1.45}.ruleline{margin:8px 0;padding:9px;background:#071321;border-radius:8px;color:#c7dcf0}.ruleline b{color:#c4b5fd}.actions{position:sticky;bottom:10px;margin-top:24px;background:#071321ee;border:1px solid var(--line);padding:12px;border-radius:12px;backdrop-filter:blur(8px)}.save{background:linear-gradient(135deg,#1769aa,#249be9);border:0;color:white;border-radius:9px;padding:12px 18px;font-weight:750}.msg{color:var(--muted)}.badge{margin-left:auto;padding:5px 8px;border-radius:99px;background:#23384a;color:var(--muted);font-size:11px}.badge.ok{background:#22c55e20;color:#4ade80}.badge.bad{background:#ef444420;color:#f87171}@media(max-width:620px){.row{grid-template-columns:1fr}.wrap{padding:13px}.actions{align-items:stretch;flex-direction:column}.save{width:100%}}
</style></head><body><div class="wrap"><div class="top"><div class="brand">MARK<span class="labs">LABS</span><span class="domain">.pl</span> / NETGUARD</div><button class="lang" id="lang">PL / EN</button></div><nav class="tabs"><a href="/">Status</a><a href="/config" id="networkTab">Sieć</a><a class="active" href="/logic" id="logicTab">Urządzenia i logika</a><a href="/home-assistant">Home Assistant</a><a href="/backup">Backup</a><a href="/update">OTA</a></nav><h1 id="title"></h1><p class="lead" id="lead"></p><form id="form"><section class="section"><div class="head"><div><div class="eyebrow powerText" id="deviceEye"></div><h2 id="deviceTitle"></h2></div></div><p class="help" id="deviceHelp"></p><div class="grid" id="devices"></div></section><section class="section"><div class="head"><div><div class="eyebrow targetText" id="targetEye"></div><h2 id="targetTitle"></h2></div><button type="button" class="ghost" id="addTarget"></button></div><p class="help" id="targetHelp"></p><div class="grid" id="targets"></div></section><section class="section"><div class="head"><div><div class="eyebrow" style="color:#a78bfa" id="ruleEye"></div><h2 id="ruleTitle"></h2></div><button type="button" class="ghost" id="addRule"></button></div><p class="help" id="ruleHelp"></p><div class="grid" id="rules"></div></section><div class="actions"><button class="save" id="save"></button><span class="msg" id="msg"></span></div></form></div><script>
const F=document.getElementById('form'),icons=['generic','router','switch','access_point','modem','server','nas','camera','internet','dns','gateway','website'],tests=['ping','tcp','http','https','dns'];let pl=localStorage.nglang!=='en',model,nextTarget=2,nextRule=0;
function svg(type){let p=type==='internet'?'<circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a15 15 0 0 1 0 18M12 3a15 15 0 0 0 0 18"/>':type==='camera'?'<path d="M4 7h11v10H4zM15 10l5-3v10l-5-3z"/><circle cx="9.5" cy="12" r="2"/>':type==='server'||type==='nas'?'<rect x="4" y="3" width="16" height="7" rx="2"/><rect x="4" y="14" width="16" height="7" rx="2"/><path d="M8 6.5h.01M8 17.5h.01M12 10v4"/>':type==='access_point'?'<path d="M5 10a10 10 0 0 1 14 0M8 13a6 6 0 0 1 8 0M11 16a2 2 0 0 1 2 0"/><circle cx="12" cy="19" r="1"/>':type==='dns'?'<path d="M12 2l8 5v10l-8 5-8-5V7zM8 9h8M8 12h8M8 15h5"/>':type==='router'||type==='modem'?'<path d="M5 9V5M19 9V5M3 11h18v8H3zM7 15h.01M11 15h.01M15 15h.01"/>':'<circle cx="12" cy="12" r="8"/><path d="M12 8v8M8 12h8"/>';return `<svg viewBox="0 0 24 24">${p}</svg>`}
function opts(list,value){return list.map((x,i)=>`<option value="${i}" ${i==value?'selected':''}>${x.replace('_',' ')}</option>`).join('')}
function deviceCard(d,i){return `<article class="card device"><div class="head"><span class="ico">${svg(icons[d.icon])}</span><div><h3>${d.name||'—'}</h3><div class="role">${pl?'URZĄDZENIE ZASILANE':'POWERED DEVICE'} · CH${d.power_channel+1}</div></div><label class="toggle"><input type="checkbox" name="d${i}_enabled" ${d.enabled?'checked':''}> ${pl?'Aktywne':'Enabled'}</label></div><label>${pl?'Nazwa urządzenia':'Device name'}</label><input name="d${i}_name" maxlength="31" value="${d.name}"><div class="row"><div><label>${pl?'Ikona':'Icon'}</label><select name="d${i}_icon">${opts(icons,d.icon)}</select></div><div><label>${pl?'Gniazdo':'Outlet'}</label><select name="d${i}_channel">${[0,1,2,3].map(x=>`<option value="${x}" ${x==d.power_channel?'selected':''}>CH${x+1}</option>`).join('')}</select></div></div><div class="row"><div><label>RESET (s)</label><input type="number" min="1" max="300" name="d${i}_pulse" value="${d.pulse_seconds}"></div><div><label>${pl?'Stabilizacja (s)':'Stabilization (s)'}</label><input type="number" min="0" max="3600" name="d${i}_stabilization" value="${d.stabilization_seconds}"></div></div><div class="row"><div><label>${pl?'Ponowna próba (s)':'Retry delay (s)'}</label><input type="number" min="0" max="86400" name="d${i}_cooldown" value="${d.cooldown_seconds}"></div><div><label>${pl?'Limit/h (0=bez)':'Limit/h (0=none)'}</label><input type="number" min="0" max="20" name="d${i}_limit" value="${d.maximum_recoveries_per_hour}"></div></div><p class="help">${pl?'RESET określa czas odłączenia zasilania. Stabilizacja blokuje kolejne akcje podczas rozruchu. Odstęp do ponownej próby i limit godzinowy chronią urządzenie przed pętlą restartów.':'RESET sets the power-off time. Stabilization blocks further actions during startup. The retry delay and hourly limit protect the device from a restart loop.'}</p></article>`}
function targetCard(t,i){return `<article class="card target"><div class="head"><span class="ico">${svg(icons[t.icon])}</span><div><h3>${t.name||'—'}</h3><div class="role">${pl?'CEL DIAGNOSTYCZNY':'DIAGNOSTIC TARGET'}</div></div><span class="badge" id="s${i}">—</span><label class="toggle"><input type="checkbox" name="t${i}_enabled" ${t.enabled?'checked':''}> ${pl?'Aktywny':'Enabled'}</label></div><label>${pl?'Nazwa celu':'Target name'}</label><input name="t${i}_name" maxlength="31" value="${t.name}"><div class="row"><div><label>${pl?'Ikona':'Icon'}</label><select name="t${i}_icon">${opts(icons,t.icon)}</select></div><div><label>${pl?'Rodzaj testu':'Test type'}</label><select name="t${i}_test">${opts(tests,t.test)}</select></div></div><label>${pl?'Adres IPv4 lub hostname':'IPv4 address or hostname'}</label><input name="t${i}_address" maxlength="63" value="${t.address}"><div class="row"><div><label>Port (0 = auto)</label><input type="number" min="0" max="65535" name="t${i}_port" value="${t.port}"></div><div><label>Timeout (ms)</label><input type="number" min="100" max="10000" name="t${i}_timeout" value="${t.timeout_ms}"></div></div><div class="row"><div><label>HTTP path</label><input name="t${i}_path" maxlength="47" value="${t.path}"></div><div><label>HTTP status</label><input type="number" min="100" max="599" name="t${i}_code" value="${t.expected_http_code}"></div></div><p class="help">${pl?'Ping sprawdza osiągalność IP. TCP sprawdza otwarcie portu. HTTP/HTTPS wymaga wskazanego kodu odpowiedzi. DNS sprawdza, czy hostname można rozwiązać. Port 0 wybiera 80 dla TCP/HTTP i 443 dla HTTPS.':'Ping checks IP reachability. TCP opens a port. HTTP/HTTPS requires the selected response code. DNS checks whether a hostname resolves. Port 0 selects 80 for TCP/HTTP and 443 for HTTPS.'}</p></article>`}
function targetOptions(value){return model.targets.map((t,i)=>`<option value="${i}" ${i==value?'selected':''}>${t.name||((pl?'Cel ':'Target ')+(i+1))}</option>`).join('')}
function deviceOptions(value){return model.devices.map((d,i)=>`<option value="${i}" ${i==value?'selected':''}>CH${d.power_channel+1} · ${d.name}</option>`).join('')}
function ruleCard(r,i){let cs='';for(let c=0;c<4;c++){let q=r.conditions[c]||{target_index:0,expected_state:0};cs+=`<div class="row"><div><label>${pl?'Warunek':'Condition'} ${c+1}</label><select name="r${i}_c${c}_target">${targetOptions(q.target_index)}</select></div><div><label>${pl?'Oczekiwany stan':'Expected state'}</label><select name="r${i}_c${c}_state"><option value="0" ${q.expected_state==0?'selected':''}>${pl?'ODPOWIADA':'UP'}</option><option value="1" ${q.expected_state==1?'selected':''}>${pl?'NIE ODPOWIADA':'DOWN'}</option></select></div></div>`}return `<article class="card rule"><div class="head"><div><h3>${r.name||((pl?'Reguła ':'Rule ')+(i+1))}</h3><div class="role">${pl?'AUTOMATYZACJA':'AUTOMATION'}</div></div><label class="toggle"><input type="checkbox" name="r${i}_enabled" ${r.enabled?'checked':''}> ${pl?'Aktywna':'Enabled'}</label></div><label>${pl?'Nazwa reguły':'Rule name'}</label><input name="r${i}_name" maxlength="39" value="${r.name}"><div class="row"><div><label>${pl?'Łączenie warunków':'Combine conditions'}</label><select name="r${i}_operator"><option value="0" ${r.operator==0?'selected':''}>${pl?'WSZYSTKIE (ORAZ)':'ALL (AND)'}</option><option value="1" ${r.operator==1?'selected':''}>${pl?'DOWOLNY (ALBO)':'ANY (OR)'}</option></select></div><div><label>${pl?'Liczba warunków':'Condition count'}</label><input type="number" min="1" max="4" name="r${i}_count" value="${r.condition_count||1}"></div></div>${cs}<div class="row"><div><label>${pl?'Kolejne cykle':'Consecutive cycles'}</label><input type="number" min="1" max="10" name="r${i}_cycles" value="${r.consecutive_cycles}"></div><div><label>${pl?'Urządzenie do restartu':'Device to restart'}</label><select name="r${i}_device">${deviceOptions(r.powered_device_index)}</select></div></div><div class="ruleline"><b>IF</b> ${pl?'warunki są spełnione':'conditions match'} → <b>RESET</b> ${model.devices[r.powered_device_index]?.name||'—'}</div><p class="help">${pl?'Liczba warunków określa, ile pozycji od góry jest używanych. Kolejne cykle filtrują chwilowe zaniki. WSZYSTKIE wymaga spełnienia każdego aktywnego warunku; DOWOLNY wymaga jednego.':'Condition count selects how many rows from the top are used. Consecutive cycles filter brief outages. ALL requires every active condition; ANY requires one.'}</p></article>`}
function draw(){devices.innerHTML=model.devices.map(deviceCard).join('');targets.innerHTML=model.targets.map((t,i)=>t.enabled||i<nextTarget?targetCard(t,i):'').join('');rules.innerHTML=model.rules.map((r,i)=>r.enabled||i<nextRule?ruleCard(r,i):'').join('')}
function tr(){document.documentElement.lang=pl?'pl':'en';networkTab.textContent=pl?'Sieć':'Network';logicTab.textContent=pl?'Urządzenia i logika':'Devices & logic';title.textContent=pl?'Kreator urządzeń i reguł':'Device and rule builder';lead.textContent=pl?'Najpierw zdefiniuj cztery fizyczne urządzenia i niezależne cele pomiarowe. Następnie połącz wyniki testów w czytelne reguły RESET.':'Define the four physical devices and independent measurement targets first. Then combine test results into clear RESET rules.';deviceEye.textContent=pl?'POMARAŃCZOWE = ZASILANIE':'ORANGE = POWER';deviceTitle.textContent=pl?'Urządzenia podłączone do gniazd':'Devices connected to outlets';deviceHelp.textContent=pl?'Każde urządzenie zajmuje dokładnie jedno fizyczne wyjście CH1–CH4. Adresy sieciowe definiuje się osobno poniżej.':'Each device occupies exactly one physical CH1–CH4 output. Network addresses are defined separately below.';targetEye.textContent=pl?'NIEBIESKIE = POMIAR':'BLUE = MEASUREMENT';targetTitle.textContent=pl?'Cele diagnostyczne':'Diagnostic targets';targetHelp.textContent=pl?'Cel jest tylko obserwowany i nie ma gniazda zasilania. Ten sam cel może występować w wielu regułach.':'A target is observation-only and has no power outlet. The same target may be used by many rules.';ruleEye.textContent=pl?'FIOLETOWE = DECYZJE':'PURPLE = DECISIONS';ruleTitle.textContent=pl?'Reguły automatyzacji':'Automation rules';ruleHelp.textContent=pl?'Reguła sprawdza stany celów przez kilka kolejnych cykli, a następnie wykonuje RESET wybranego urządzenia.':'A rule checks target states for several consecutive cycles and then resets the selected device.';addTarget.textContent=pl?'+ Dodaj cel':'+ Add target';addRule.textContent=pl?'+ Dodaj regułę':'+ Add rule';save.textContent=pl?'Zapisz całą logikę':'Save complete logic';if(model)draw()}
function captureForm(){if(!model)return;let d=new FormData(F),num=(k,v)=>d.has(k)?Number(d.get(k)):v,str=(k,v)=>d.has(k)?String(d.get(k)):v;model.devices.forEach((x,i)=>{let p='d'+i+'_';x.enabled=d.has(p+'enabled');x.name=str(p+'name',x.name);x.icon=num(p+'icon',x.icon);x.power_channel=num(p+'channel',x.power_channel);x.pulse_seconds=num(p+'pulse',x.pulse_seconds);x.stabilization_seconds=num(p+'stabilization',x.stabilization_seconds);x.cooldown_seconds=num(p+'cooldown',x.cooldown_seconds);x.maximum_recoveries_per_hour=num(p+'limit',x.maximum_recoveries_per_hour)});model.targets.forEach((x,i)=>{let p='t'+i+'_';if(!d.has(p+'name'))return;x.enabled=d.has(p+'enabled');x.name=str(p+'name',x.name);x.icon=num(p+'icon',x.icon);x.address=str(p+'address',x.address);x.test=num(p+'test',x.test);x.port=num(p+'port',x.port);x.path=str(p+'path',x.path);x.expected_http_code=num(p+'code',x.expected_http_code);x.timeout_ms=num(p+'timeout',x.timeout_ms)});model.rules.forEach((x,i)=>{let p='r'+i+'_';if(!d.has(p+'name'))return;x.enabled=d.has(p+'enabled');x.name=str(p+'name',x.name);x.operator=num(p+'operator',x.operator);x.condition_count=num(p+'count',x.condition_count);x.consecutive_cycles=num(p+'cycles',x.consecutive_cycles);x.powered_device_index=num(p+'device',x.powered_device_index);x.conditions.forEach((q,c)=>{q.target_index=num(p+'c'+c+'_target',q.target_index);q.expected_state=num(p+'c'+c+'_state',q.expected_state)})})}
addTarget.onclick=()=>{captureForm();if(nextTarget<12)nextTarget++;draw()};addRule.onclick=()=>{captureForm();if(nextRule<8)nextRule++;draw()};lang.onclick=()=>{captureForm();pl=!pl;localStorage.nglang=pl?'pl':'en';tr()};function errorText(code){let m={FORM_DATA_TRUNCATED:pl?'Formularz był zbyt duży i został odrzucony bez zapisu.':'The form was too large and was rejected without saving.',RESET_IN_PROGRESS:pl?'Nie można zmieniać mapowania podczas trwającego RESET.':'Mapping cannot be changed while RESET is in progress.',DUPLICATE_POWER_CHANNEL:pl?'Jedno wyjście CH może zasilać tylko jedno aktywne urządzenie.':'One CH output can power only one enabled device.',INVALID_POWERED_DEVICE:pl?'Sprawdź nazwy, kanały i czasy urządzeń zasilanych.':'Check powered device names, channels, and timing.',INVALID_DIAGNOSTIC_TARGET:pl?'Sprawdź nazwę, adres i parametry celu diagnostycznego.':'Check the diagnostic target name, address, and parameters.',INVALID_AUTOMATION_RULE:pl?'Reguła jest niekompletna albo wskazuje wyłączone urządzenie.':'The rule is incomplete or targets a disabled device.',INVALID_RULE_TARGET:pl?'Reguła wskazuje nieaktywny lub nieistniejący cel.':'The rule references an inactive or missing target.',NVS_WRITE_FAILED:pl?'Nie udało się bezpiecznie zapisać konfiguracji.':'Configuration could not be stored safely.'};return m[code]||code}F.onsubmit=async e=>{e.preventDefault();save.disabled=true;msg.textContent=pl?'Zapisywanie…':'Saving…';try{let r=await fetch('/api/v1/logic',{method:'POST',body:new URLSearchParams(new FormData(F))}),x=await r.json();msg.textContent=r.ok?(pl?'Zapisano poprawnie · zapis #':'Saved successfully · revision #')+(x.sequence||'—'):(pl?'Błąd zapisu: ':'Save error: ')+errorText(x.error||r.status);if(r.ok)await load()}catch(e){msg.textContent=pl?'Brak odpowiedzi urządzenia.':'No response from device.'}finally{save.disabled=false}};async function load(){model=await fetch('/api/v1/logic',{cache:'no-store'}).then(r=>r.json());nextTarget=Math.max(2,model.targets.reduce((n,t,i)=>t.enabled?i+1:n,0));nextRule=model.rules.reduce((n,r,i)=>r.enabled||r.name?i+1:n,0);tr()}async function loadStatus(){try{let s=await fetch('/api/v1/logic/status',{cache:'no-store'}).then(r=>r.json());s.targets.forEach((t,i)=>{let b=document.getElementById('s'+i);if(!b)return;b.className='badge '+(t.ok?'ok':'bad');b.textContent=!t.enabled?'—':t.ok?'OK · '+t.response_ms+' ms':(pl?'BŁĄD':'FAIL')})}catch(e){}}tr();load().then(loadStatus);setInterval(loadStatus,3000);
</script></body></html>)HTML";
}
